<p id="notice"><%= notice %></p>
<div id="createData"></div>
<h1>Markers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody id="data">
    <% @markers.each do |m| %>
            <%= render 'data', m: m %>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Marker', new_marker_path, remote: true %>

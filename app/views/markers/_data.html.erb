<tr id="marker_<%= m.id %>">
    <td><%= m.name %></td> 
    <td><%= m.url %></td>
    <td><%= link_to 'Show', m %></td>
    <td><%= link_to 'Edit', edit_marker_path(m) %></td>
    <td><%= link_to 'Destroy', m, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>